"""Engine version this package was built against. Overwritten by CI at
wheel-build time with the release tag; the dev placeholder below is used
only for local builds, where phys resolve from .pio/build/native_lib and
no download is attempted."""
ENGINE_VERSION = "0.0.0+dev"
